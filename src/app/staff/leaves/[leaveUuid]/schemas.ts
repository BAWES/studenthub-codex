// ---------------------------------------------------------------------------
// Staff — Leaves Detail Page Schemas
// ---------------------------------------------------------------------------
// Re-exported from the module-level schemas so the page test can
// validate the data contract without importing from a deep module path.
// ---------------------------------------------------------------------------

export { staffLeaveListItemSchema } from "@/modules/staff-leaves/schemas";
