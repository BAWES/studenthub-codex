// ---------------------------------------------------------------------------
// Empty state messages for workspace components
// ---------------------------------------------------------------------------

/** Shown on a dashboard when there is no recent activity to display. */
export const EMPTY_NO_RECENT_ACTIVITY = "No recent activity to display.";

/** Shown on a detail page when there are no related records for the entity. */
export const EMPTY_NO_RELATED_RECORDS = "No related records found.";

/** Shown on a data-table page when no records exist at all. */
export const EMPTY_NO_RECORDS = "No records found.";

/** Default hint displayed alongside the no-records message. */
export const EMPTY_HINT_DEFAULT =
  "Create a new record or adjust your filters to see results here.";

/** Shown when a search query produces no results. */
export const emptyNoResults = {
  title: "No results found",
  description: "Try a different search term or clear your filter.",
};
