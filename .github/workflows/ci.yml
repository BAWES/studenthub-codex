name: StudentHub CI

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main, develop]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  branch-name:
    name: Branch Name Check
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - name: Validate branch name
        run: |
          BRANCH="${{ github.head_ref }}"
          echo "Checking branch: $BRANCH"
          if echo "$BRANCH" | grep -qiE '^(feature|fix|chore|coder2)/STU-[a-z0-9]+[-a-z0-9]+$'; then
            echo "Branch name OK: $BRANCH"
          elif echo "$BRANCH" | grep -qiE '^(ux|uxdesigner)/[a-z0-9-]+$'; then
            echo "Branch name OK (UX): $BRANCH"
          else
            echo "Branch name REJECTED: $BRANCH"
            echo "Expected: feature/STU-N-*, fix/STU-N-*, chore/STU-N-*, or coder2/STU-N-* (kebab-case) — UX: ux/* or uxdesigner/*"
            exit 1
          fi

  clean-tree:
    name: Clean Working Tree
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
      - run: npm ci
      - name: Check for uncommitted changes
        run: |
          if ! git diff --exit-code; then
            echo "Working tree is dirty after npm ci — uncommitted changes detected"
            exit 1
          fi
          echo "Working tree is clean"

  typescript:
    name: TypeScript Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
      - run: npm ci
      - run: npx tsc --noEmit

  build:
    name: Next.js Build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
      - run: npm ci
      - name: Generate Prisma client
        run: npx prisma generate
      - name: Build
        run: npx next build

  lint:
    name: ESLint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
      - run: npm ci
      - run: npm run lint

  validate:
    name: Validation Suite
    runs-on: ubuntu-latest
    timeout-minutes: 10
    services:
      mysql:
        image: mysql:8.4
        env:
          MYSQL_ROOT_PASSWORD: studenthub
          MYSQL_DATABASE: studenthub_prod_local
          MYSQL_USER: studenthub
          MYSQL_PASSWORD: studenthub
        ports:
          - 3307:3306
        options: >-
          --health-cmd="mysqladmin ping -h localhost"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
      - run: npm ci
      - name: Generate Prisma client
        run: npx prisma generate
      - name: Push schema to MySQL
        run: npx prisma db push --skip-generate
        env:
          DATABASE_URL: "mysql://studenthub:studenthub@localhost:3307/studenthub_prod_local"
      - name: Build
        run: npx next build
      - name: Start and validate
        run: |
          npm run dev &
          # Wait for dev server to be ready
          for i in $(seq 1 20); do
            if curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/login | grep -q "200\\|307"; then
              echo "Dev server ready after ${i}s"
              break
            fi
            sleep 2
          done
          node scripts/validate.mjs
        env:
          DATABASE_URL: "mysql://studenthub:studenthub@localhost:3307/studenthub_prod_local"

  e2e:
    name: Playwright E2E
    runs-on: ubuntu-latest
    timeout-minutes: 20
    services:
      mysql:
        image: mysql:8.4
        env:
          MYSQL_ROOT_PASSWORD: studenthub
          MYSQL_DATABASE: studenthub_prod_local
          MYSQL_USER: studenthub
          MYSQL_PASSWORD: studenthub
        ports:
          - 3307:3306
        options: >-
          --health-cmd="mysqladmin ping -h localhost"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "npm"
      - run: npm ci
      - name: Generate Prisma client
        run: npx prisma generate
      - name: Push schema to MySQL
        run: npx prisma db push --skip-generate
        env:
          DATABASE_URL: "mysql://studenthub:studenthub@localhost:3307/studenthub_prod_local"
      - name: Seed E2E test data
        run: mysql -h 127.0.0.1 -P 3307 -u studenthub -pstudenthub studenthub_prod_local < scripts/seed-e2e.sql
      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium
      - name: Build Next.js
        run: npx next build
        env:
          DATABASE_URL: "mysql://studenthub:studenthub@localhost:3307/studenthub_prod_local"

  e2e:
    name: Playwright E2E
    runs-on: ubuntu-latest
    timeout-minutes: 20
    services:
      mysql:
        image: mysql:8.4
        env:
          MYSQL_ROOT_PASSWORD: studenthub
          MYSQL_DATABASE: studenthub_prod_local
          MYSQL_USER: studenthub
          MYSQL_PASSWORD: studenthub
        ports:
          - 3307:3306
        options: >-
          --health-cmd="mysqladmin ping -h localhost"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "npm"
      - run: npm ci
      - name: Generate Prisma client
        run: npx prisma generate
      - name: Push schema to MySQL
        run: npx prisma db push --skip-generate
        env:
          DATABASE_URL: "mysql://studenthub:studenthub@localhost:3307/studenthub_prod_local"
      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium
      - name: Run Playwright E2E tests
        run: npx playwright test
        env:
          CI: "true"
          AUTH_SECRET: ${{ secrets.AUTH_SECRET }}
          DATABASE_URL: "mysql://studenthub:studenthub@localhost:3307/studenthub_prod_local"
      - name: Upload Playwright report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7

